import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.karathril.pruebamovil',
  appName: 'prueba_movil',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
